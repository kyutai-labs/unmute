apiVersion: v1
kind: Namespace
metadata:
  name: speech-io
---
# Note: We use emptyDir volumes instead of PVCs since the GPU node
# doesn't support Longhorn storage. Shared volumes are defined in
# the deployments themselves using emptyDir with shared naming.
